import { Tablr } from '../Tablr';

export default {
    title: 'Tablr - A Customizable Table Component',
    component: Tablr,
};

const Template = (args) => <Tablr />;

export const Default = Template.bind({});
